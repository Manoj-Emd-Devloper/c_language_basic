//wap for check the byte of an integer datatype
#include <stdio.h>

int main() {
    printf("The size of int is = %d \n", sizeof(int));
    return 0;
}
    //sizeof is unary operator and not a function.
    //it's uses for know the size of datatype