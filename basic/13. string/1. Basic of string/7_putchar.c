#include <stdio.h>

int main() {
    int ch;
    for(ch=65 ; ch<=90 ; ch++)
        putchar(ch); //use for print and to change the integer value to ascii code 
        //prototype int putchar (int ch);
    return 0;
}